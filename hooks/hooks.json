{
  "description": "claude-video — checks for ffmpeg dependency at session start",
  "hooks": {
    "SessionStart": [{
      "hooks": [{
        "type": "command",
        "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/session_start.sh",
        "timeout": 5
      }]
    }]
  }
}
